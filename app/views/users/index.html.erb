<h1>
  this is a list of all users
</h1>

<%= will_paginate %>

  <ul class="users">
    <% @users.each do |u| %>
      <li>
        <%= gravatar_for u, size: 50 %>
        <%= link_to u.name, user_path(u) %>
      </li>
    <% end %>
  </ul>

<%= will_paginate %>
